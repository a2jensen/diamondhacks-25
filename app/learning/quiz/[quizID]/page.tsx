export default function Quiz( quizId : string ){
    // function call to fetch the quiz data from the backend here and populate
    return (
        <div>This is the custom quiz page that you opened up, for the specific quiz</div>
    )
}